# Application environment example.
# Add only non-secret variable names required by the application.
#
# Required by the hermes-finance CLI at every invocation: the path of the
# finance SQLite database file. No default path exists; a missing or blank
# value is a deterministic CLI error.
#
# HERMES_FINANCE_DB_PATH=/path/to/finance.db
